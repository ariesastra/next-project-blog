// Styles
import {container, title} from './styles'

const Header = () => (
    <header css={container}>
        <h1 css={title}>Welcome to Next Project Blog</h1>
    </header>
)

export default Header